let { defineSupportCode } = require("cucumber");

let assert = require("assert");

defineSupportCode(function ({ Given, Then, When }) {
    Given('发送post请求请求参数为', function (docString) {
        
    });

    When('返回结果中应该有', function (docString) {
       
    });
})